import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    marginVertical: 16,
    fontWeight: 'bold',
    fontSize: 14,
  },
  flatList: {
    width: '100%',
    maxWidth: 400,
  },
});

export default styles;
