import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  flatList: {
    width: '100%',
    maxWidth: 400,
  },
  text: {
    fontSize: 20,
    fontWeight: 'bold',
  }
});

export default styles
